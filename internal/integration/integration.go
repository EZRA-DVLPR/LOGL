package integration

import (
	"fmt"
)

func GetAllGamesGOG() {
	// require the user to login to gog with cookies enabled
	// then visit link: https://embed.gog.com/account/getFilteredProducts?mediaType=1&page=1 for all pages
	// and save the json data into a file themself
	// which is then parsed by this function
}
